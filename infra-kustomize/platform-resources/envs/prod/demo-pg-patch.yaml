apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-pg
  namespace: demo-app
spec:
  instances: 3
  storage:
    size: 20Gi
  bootstrap:
    initdb:
      database: demo_prod
  postgresql:
    parameters:
      max_connections: "200"
