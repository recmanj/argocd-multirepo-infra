apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-local-pg
  namespace: demo-app-local
spec:
  instances: 3
  storage:
    size: 20Gi
  bootstrap:
    initdb:
      database: demo_local_prod
  postgresql:
    parameters:
      max_connections: "200"
