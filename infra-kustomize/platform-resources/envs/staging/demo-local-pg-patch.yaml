apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-local-pg
  namespace: demo-app-local
spec:
  instances: 2
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: demo_local_staging
  postgresql:
    parameters:
      max_connections: "100"
