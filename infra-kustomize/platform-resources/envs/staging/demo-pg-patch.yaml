apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-pg
  namespace: demo-app
spec:
  instances: 2
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: demo_staging
  postgresql:
    parameters:
      max_connections: "100"
