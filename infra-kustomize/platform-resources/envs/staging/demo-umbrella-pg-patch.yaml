apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-umbrella-pg
  namespace: demo-app-umbrella
spec:
  instances: 2
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: demo_umbrella_staging
  postgresql:
    parameters:
      max_connections: "100"
