apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-chart-pg
  namespace: demo-app-chart
spec:
  instances: 2
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: demo_chart_staging
  postgresql:
    parameters:
      max_connections: "100"
